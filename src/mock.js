/*
const Mock = require("mockjs")
/*var data = Mock.mock({
    'list|1-3': [{// 属性 list 的值是一个数组，其中含有 1 到 10 个元素
        'id|+1': 1, // 属性 id 是一个自增数，起始值为 1，每次增 1
        'username|1':"11",  //字符串
        'data':1
    }]
})
const Random = Mock.Random;
Mock.mock('/demo/login','post',{
    'list|1': [{// 属性 list 的值是一个数组，其中含有 1 到 10 个元素
        'id|+1': 1, // 属性 id 是一个自增数，起始值为 1，每次增 1
        'username|1':"11",  //字符串
        'data':1
    }]
})
Mock.mock('/demo/register','post',{
    'list': [{// 属性 list 的值是一个数组，其中含有 1 到 10 个元素
        'status|1':"200",  //注册成功返回200

    }]
})
Mock.mock('/demo/user/1','post',{
    'list|1': [{// 属性 list 的值是一个数组，其中含有 1 到 10 个元素
        'id|+1': 1, // 属性 id 是一个自增数，起始值为 1，每次增 1
        'username|1':"11",  //字符串
        'email':"111",
        'phone':"11"
    }]
})
*/
